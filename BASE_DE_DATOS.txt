-- DELETES --

USE GESTION_TALLER;

IF OBJECT_ID('BORRAR_ESTUDIANTE', 'P') IS NOT NULL
    DROP PROCEDURE BORRAR_ESTUDIANTE;
GO

CREATE PROCEDURE BORRAR_ESTUDIANTE --puede que no lo vuelva a usar, en vez de borrar estudiantes de los talleres, seria mejor colocarlos como inactivos, almacen de datos.
 @ID_ESTUDIANTE NUMERIC(10),
 @ID_TALLER NUMERIC(10)
AS BEGIN
	DELETE FROM SGT_INTEGRANTE_TALLER
	WHERE ID_ESTUDIANTE=@ID_ESTUDIANTE
	AND (@ID_TALLER IS NULL OR ID_TALLER=@ID_TALLER);

    DELETE FROM SGT_LISTA_ESPERA
    WHERE ID_ESTUDIANTE=@ID_ESTUDIANTE
    AND ID_TALLER=@ID_TALLER;

    IF @@ROWCOUNT > 0
        SELECT 'Probando wuaaa '+CAST(@ID_ESTUDIANTE AS VARCHAR) AS RESULTADO;
    ELSE
        SELECT 'En caso de que no funcione sale este mensaje '+CAST(@ID_ESTUDIANTE AS VARCHAR) AS RESULTADO;
END

----

USE GESTION_TALLER;

IF OBJECT_ID('BORRAR_PROFESOR', 'P') IS NOT NULL
    DROP PROCEDURE BORRAR_PROFESOR; --puede que no lo vuelva a usar, en vez de borrar estudiantes de los talleres, seria mejor colocarlos como inactivos, almacen de datos.
GO

CREATE PROCEDURE BORRAR_PROFESOR --falta actualizar, pero no es prioridad hasta tener primero lo de estudiante terminado
 @ID_PROFESOR INT
AS BEGIN
	DELETE FROM SGT_PROFESOR
	WHERE ID_PROFESOR=@ID_PROFESOR;
	SELECT 'Los datos del profesor/a fueron eliminados' AS RESULTADO;
END;

----

USE GESTION_TALLER;

IF OBJECT_ID('BORRAR_TALLER', 'P') IS NOT NULL
    DROP PROCEDURE BORRAR_TALLER; --puede que no lo vuelva a usar, en vez de borrar estudiantes de los talleres, seria mejor colocarlos como inactivos, almacen de datos.
GO

CREATE PROCEDURE BORRAR_TALLER --falta actualizar, pero no es prioridad hasta tener primero lo de estudiante terminado
 @ID_TALLER NUMERIC(10)
AS BEGIN
	DELETE FROM SGT_TALLER
	WHERE ID_TALLER=@ID_TALLER;
	SELECT 'Los datos del taller fueron eliminados' AS RESULTADO;
END;

----

-- TABLA PERSONA --

USE GESTION_TALLER;

IF OBJECT_ID('SGT_PERSONA_TALLER', 'U') IS NOT NULL
    DROP TABLE SGT_PERSONA_TALLER;
GO

CREATE TABLE SGT_PERSONA_TALLER(
    ID_PERSONA NUMERIC (10,0) IDENTITY PRIMARY KEY NOT NULL,
    RUT_PERSONA NUMERIC (10,0),
    DV_PERSONA CHAR (1),
    PASAPORTE VARCHAR (30),
    NOMBRE_PERSONA VARCHAR (50) NOT NULL,
    APELLIDO_PATERNO VARCHAR (50) NOT NULL,
    APELLIDO_MATERNO VARCHAR (50) NOT NULL,
    FEC_NACIMIENTO DATE NOT NULL,
    EDAD INT,
    GENERO CHAR (1),
    TELEFONO VARCHAR (20) NOT NULL,
    CORREO_ELECTRONICO VARCHAR (100) NOT NULL,
    CALLE VARCHAR (100) NOT NULL,
    NRO_CALLE VARCHAR (10),
    NRO_BLOCK VARCHAR (10),
    NRO_DPTO VARCHAR (10),
    VILLA VARCHAR (100),
    ID_COMUNA INT NOT NULL,
    ID_PAIS INT NOT NULL,
    TELEFONO_CONTACTO VARCHAR (20),
    NOMBRE_CONTACTO VARCHAR (100),
    CORREO_CONTACTO VARCHAR (100),
    OBSERVACION VARCHAR (500),
    CONTRASENA_HASH VARCHAR (255) NOT NULL,
    TIPO_USUARIO VARCHAR (20) NOT NULL DEFAULT 'FUNCIONARIO', 
    --los tipos de usuario son 'FUNCIONARIO' o 'ADMIN'
    IND_ACTIVO BIT NOT NULL DEFAULT 1,
    FEC_CREACION DATETIME DEFAULT GETDATE(),
    ULTIMO_ACCESO DATETIME DEFAULT GETDATE(),
    INTENTOS_FALLIDOS INT DEFAULT 0,
    BLOQUEADO_HASTA DATETIME,
    AUD_USUARIO_INGRESO VARCHAR (20) DEFAULT 'SISTEMA',
    AUD_FEC_INGRESO DATETIME DEFAULT GETDATE(),
    AUD_FEC_MODIFICA DATETIME,
    AUD_USUARIO_MODIFICA VARCHAR (20)
);

CREATE INDEX IX_PERSONA_RUT ON SGT_PERSONA_TALLER(RUT_PERSONA);
CREATE INDEX IX_PERSONA_CORREO ON SGT_PERSONA_TALLER(CORREO_ELECTRONICO);
CREATE INDEX IX_PERSONA_TIPO ON SGT_PERSONA_TALLER(TIPO_USUARIO, IND_ACTIVO);

GO

--INSERT INTO SGT_PERSONA_TALLER (RUT_PERSONA,DV_PERSONA,NOMBRE_PERSONA,APELLIDO_PATERNO,APELLIDO_MATERNO,FEC_NACIMIENTO,TELEFONO,CORREO_ELECTRONICO,CALLE,ID_COMUNA,ID_PAIS,CONTRASENA_HASH)
--VALUES (123123,1,'PRUEBA','AP_PRUEBA','AM_PRUEBA','01-08-2001','4444','PRUEBA@GMAIL.COM','WUAP',1,1,'ESQUERE')

----

-- VER PERSONAS EN LOS TALLERES(VER ESTUDIANTE ANTERIOR, TIENE QUE SER CAMBIADO) --

USE GESTION_TALLER;

IF OBJECT_ID('VER_ESTUDIANTE_TALLER', 'P') IS NOT NULL
    DROP PROCEDURE VER_ESTUDIANTE_TALLER;
GO

CREATE PROCEDURE VER_ESTUDIANTE_TALLER
	@ID_ESTUDIANTE NUMERIC(10),
	@ID_TALLER NUMERIC(10) = NULL

AS BEGIN
	SELECT E.ID_TALLER, T.NOMBRE_TALLER, E.ID_ESTUDIANTE, E.YEAR_PROCESO, E.IND_ESTADO_INTEGRANTE, E.FEC_INSCRIPCION, E.FEC_RETIRO, E.OBSERVACION, E.FEC_REINCORPORACION, E.AUD_USUARIO_INGRESO, E.AUD_FEC_INGRESO, E.AUD_USUARIO_MODIFICA, E.AUD_FEC_MODIFICA
	FROM SGT_INTEGRANTE_TALLER E
	INNER JOIN SGT_TALLER T
		ON E.ID_TALLER = T.ID_TALLER
	WHERE E.ID_ESTUDIANTE = @ID_ESTUDIANTE
		AND (@ID_TALLER IS NULL OR E.ID_TALLER = @ID_TALLER)
	ORDER BY E.FEC_INSCRIPCION DESC;
END;

----

-- ACTUALIZAR PERSONAS EN LOS TALLERES(ACTUALIZAR ESTUDIANTE ANTERIOR, TIENE QUE SER CAMBIADO) --

USE GESTION_TALLER;

IF OBJECT_ID('AC_ESTUDIANTE', 'P') IS NOT NULL
    DROP PROCEDURE AC_ESTUDIANTE;
GO

CREATE PROCEDURE AC_ESTUDIANTE
 @ID_TALLER NUMERIC(10),
 @ID_ESTUDIANTE NUMERIC(10),
 @YEAR_PROCESO INT,
 @IND_ESTADO_INTEGRANTE INT,
 @FEC_INSCRIPCION DATETIME = NULL,
 @FEC_RETIRO DATETIME = NULL,
 @OBSERVACION TEXT = NULL,
 @FEC_REINCORPORACION DATETIME = NULL,
 @AUD_USUARIO_INGRESO VARCHAR(20),
 @AUD_FEC_INGRESO DATETIME = NULL,
 @AUD_USUARIO_MODIFICA VARCHAR(20),
 @AUD_FEC_MODIFICA DATETIME = NULL
AS BEGIN

UPDATE SGT_INTEGRANTE_TALLER
SET IND_ESTADO_INTEGRANTE=@IND_ESTADO_INTEGRANTE,
	FEC_RETIRO=@FEC_RETIRO,
	OBSERVACION=@OBSERVACION,
	FEC_REINCORPORACION=@FEC_REINCORPORACION,
	AUD_USUARIO_MODIFICA=@AUD_USUARIO_MODIFICA,
	AUD_FEC_MODIFICA=ISNULL(@AUD_FEC_MODIFICA, GETDATE())
WHERE ID_ESTUDIANTE=@ID_ESTUDIANTE 
  AND ID_TALLER=@ID_TALLER 
  AND YEAR_PROCESO=@YEAR_PROCESO;

IF @@ROWCOUNT > 0
    SELECT 'Se han actualizado los datos del/la "Estudiante" al sistema' AS RESULTADO;
ELSE
    SELECT 'No se encontró el registro especificado' AS RESULTADO;
END;

----

-- TRIGGER DE CALCULO DE EDAD(AUN UTIL) --

USE GESTION_TALLER;

IF OBJECT_ID('TR_SGT_PERSONA_CALCULAR_EDAD', 'TR') IS NOT NULL
    DROP TRIGGER TR_SGT_PERSONA_CALCULAR_EDAD;
GO

CREATE TRIGGER TR_SGT_PERSONA_CALCULAR_EDAD
ON SGT_PERSONA_TALLER
AFTER INSERT, UPDATE
AS BEGIN
	SET NOCOUNT ON;
	--servira para contar la edad del usuario calculando segun su nacimiento.
	UPDATE P
	SET EDAD = DATEDIFF(YEAR, I.FEC_NACIMIENTO, GETDATE())
	FROM SGT_PERSONA_TALLER P
		INNER JOIN INSERTED I ON P.ID_PERSONA = I.ID_PERSONA
	WHERE P.FEC_NACIMIENTO IS NOT NULL;
END;
GO

----

-- LISTA DE ESPERA --

USE GESTION_TALLER;

IF OBJECT_ID('SGT_LISTA_ESPERA', 'U') IS NOT NULL
    DROP TABLE SGT_LISTA_ESPERA;
GO

CREATE TABLE SGT_LISTA_ESPERA(
    ID_ESPERA NUMERIC IDENTITY(1,1) PRIMARY KEY NOT NULL,
    ID_TALLER NUMERIC(10,0) NOT NULL,
    ID_ESTUDIANTE NUMERIC(10,0) NOT NULL,
    YEAR_PROCESO INT NOT NULL,
    IND_ESTADO_INTEGRANTE INT NOT NULL,
    FECHA_ESPERA DATETIME NOT NULL DEFAULT GETDATE(),
    ESTADO VARCHAR(20) NOT NULL DEFAULT 'En Espera',
    
    CONSTRAINT FK_LISTA_ESPERA_SGT_TALLER
        FOREIGN KEY (ID_TALLER)
        REFERENCES SGT_TALLER(ID_TALLER),

    CONSTRAINT FK_LISTA_ESPERA_SGT_ESTUDIANTE
        FOREIGN KEY (ID_ESTUDIANTE)
        REFERENCES SGT_ESTUDIANTE(ID_ESTUDIANTE)
);
GO

----

-- AUTENTICADOR --

USE GESTION_TALLER;

IF OBJECT_ID('AUTENTIFICACION', 'P') IS NOT NULL
    DROP PROCEDURE AUTENTIFICACION;
GO

CREATE PROCEDURE AUTENTIFICACION
    @IDENTIFICADOR VARCHAR(100),
    @CONTRASENA VARCHAR(255)
AS BEGIN
    SET NOCOUNT ON;

    DECLARE @CONTRASENA_HASH VARCHAR (255)
        SET @CONTRASENA_HASH = CONVERT(VARCHAR(255), HASHBYTES('SHA2_256', @CONTRASENA));
    
    DECLARE @ID_PERSONA NUMERIC(10,0);
    DECLARE @TIPO_USUARIO VARCHAR(20);
    DECLARE @NOMBRE_COMPLETO VARCHAR(200);
    DECLARE @BLOQUEADO BIT;
    DECLARE @INTENTOS INT;

    SELECT
        @ID_PERSONA = ID_PERSONA,
        @TIPO_USUARIO = TIPO_USUARIO,
        @NOMBRE_COMPLETO = NOMBRE_PERSONA + ' ' + APELLIDO_PATERNO + ' ' + APELLIDO_MATERNO,
        @BLOQUEADO = CASE WHEN BLOQUEADO_HASTA IS NOT NULL AND BLOQUEADO_HASTA > GETDATE() THEN 1 ELSE 0 END,
        @INTENTOS = INTENTOS_FALLIDOS
    FROM SGT_PERSONA_TALLER
    WHERE (RUT_PERSONA = CASE WHEN ISNUMERIC(@IDENTIFICADOR) = 1 THEN CAST(@IDENTIFICADOR AS NUMERIC(10,0)) ELSE NULL END OR CORREO_ELECTRONICO = @IDENTIFICADOR) AND IND_ACTIVO = 1;
    --WHERE (RUT_PERSONA = TRY_CAST(@IDENTIFICADOR AS NUMERIC) OR CORREO_ELECTRONICO = @IDENTIFICADOR) AND IND_ACTIVO = 1; 
    --  la base de datos es de 2008 la r2 asi que xd

    IF @ID_PERSONA IS NULL
    BEGIN
        SELECT 'ERROR' AS STATUS, 'USUARIO NO ENCONTRADO' AS MESSAGE, NULL AS DATOS;
        RETURN;
    END

    IF @BLOQUEADO = 1
    BEGIN
        SELECT 'ERROR' AS STATUS,
        'CUENTA BLOQUEADA TEMPORALMENTE' AS MESSAGE;
        RETURN;
    END

    IF @INTENTOS >= 3
    BEGIN
        UPDATE SGT_PERSONA_TALLER
        SET BLOQUEADO_HASTA = DATEADD(MINUTE, 1, GETDATE())
        WHERE ID_PERSONA = @ID_PERSONA;

        SELECT 'ERROR' AS STATUS, 'DEMASIADOS INTENTOS, BLOQUEADO POR 1 MINUTO' AS MESSAGE, NULL AS DATOS;
        RETURN;
    END

---- Source - https://stackoverflow.com/a/41015373
---- Posted by Rich Benner, modified by community. See post 'Timeline' for change history
---- Retrieved 2026-02-04, License - CC BY-SA 4.0

--IF EXISTS (SELECT * FROM tblGLUserAccess WHERE GLUserName ='xxxxxxxx') 
--BEGIN
--   SELECT 1 
--END
--ELSE
--BEGIN
--    SELECT 2
--END


    IF EXISTS (SELECT 1 FROM SGT_PERSONA_TALLER WHERE ID_PERSONA = @ID_PERSONA AND CONTRASENA_HASH = @CONTRASENA_HASH)
    BEGIN
        UPDATE SGT_PERSONA_TALLER
        SET ULTIMO_ACCESO = GETDATE(),
            INTENTOS_FALLIDOS = 0,
            BLOQUEADO_HASTA = NULL,
            AUD_USUARIO_MODIFICA = 'SISTEMA',
            AUD_FEC_MODIFICA = GETDATE()
        WHERE ID_PERSONA = @ID_PERSONA;

        DECLARE @ID_ROL NUMERIC (10,0);

        IF @TIPO_USUARIO = 'ESTUDIANTE'
        BEGIN
            SELECT @ID_ROL = ID_ESTUDIANTE
            FROM SGT_ESTUDIANTE
            WHERE ID_PERSONA = @ID_PERSONA;
        END

        ELSE IF @TIPO_USUARIO = 'PROFESOR'
        BEGIN
            SELECT @ID_ROL = ID_PROFESOR
            FROM SGT_PROFESOR
            WHERE ID_PERSONA = @ID_PERSONA;
        END

        ELSE IF @TIPO_USUARIO = 'ADMIN'
        BEGIN
            SET @ID_ROL = @ID_PERSONA;
        END

        SELECT
            'BIEN' AS STATUS, 
            'AUTENTIFICACION EXITOSA' AS MESSAGE,
            @ID_PERSONA AS ID_PERSONA,
            @TIPO_USUARIO AS TIPO_USUARIO,
            @ID_ROL AS ID_ROL,
            @NOMBRE_COMPLETO AS NOMBRE_COMPLETO;
        END
        --SELECT 
        --    'BIEN' AS STATUS, 
        --    'AUTENTIFICACION EXITOSA' AS MESSAGE,
        --    JSON_QUERY((
        --        SELECT 
        --            P.ID_PERSONA, 
        --            P.RUT_PERSONA, 
        --            P.DV_PERSONA, 
        --            P.NOMBRE_PERSONA, 
        --            P.APELLIDO_PATERNO, 
        --            P.APELLIDO_MATERNO, 
        --            P.CORREO_ELECTRONICO, 
        --            P.TIPO_USUARIO, 
        --            @ID_ROL AS ID_ROL, 
        --            @NOMBRE_COMPLETO AS NOMBRE_COMPLETO, 
        --            P.TELEFONO, 
        --            P.FEC_NACIMIENTO, 
        --            P.EDAD, 
        --            P.GENERO
        --        FROM SGT_PERSONA_TALLER P
        --        WHERE P.ID_PERSONA = @ID_PERSONA 
        --        FOR JSON PATH, WITHOUT_ARRAY_WRAPPER
        -- )) AS DATOS;

--SELECT JSON_QUERY(@j, '$.credit_cards[*].type' WITH ARRAY WRAPPER);

--SELECT TOP 3 SalesOrderNumber, OrderDate, Status  
--FROM Sales.SalesOrderHeader  
--ORDER BY ModifiedDate  
--FOR JSON PATH, WITHOUT_ARRAY_WRAPPER

        ELSE
        BEGIN
            UPDATE SGT_PERSONA_TALLER
            SET INTENTOS_FALLIDOS = INTENTOS_FALLIDOS + 1,
                AUD_USUARIO_INGRESO ='SISTEMA',
                AUD_FEC_MODIFICA = GETDATE()
                WHERE ID_PERSONA = @ID_PERSONA;
                SELECT 'ERROR' AS STATUS, 'CONTRASEÑA INCORRECTA' AS MESSAGE, NULL AS DATOS;
        END
END;
GO

----

-- REGISTRAR PERSONA --

    use GESTION_TALLER;
--explicacion de que sucede aqui, este procedure deberia servir para añadir datos a la tabla de persona que cree, estudiante y profesor hacen uso de la id persona pero no tengo acceso
--asi que con esto ademas de tabla deberia funcionar todo bien, gracias a esta madr se deberia crear tanto estudiantes como profesor para despues darles usos a sus nombres , rut , etc

IF OBJECT_ID('REGISTRAR_PERSONA', 'P') IS NOT NULL --util
    DROP PROCEDURE REGISTRAR_PERSONA;
GO

CREATE PROCEDURE REGISTRAR_PERSONA
    @RUT_PERSONA NUMERIC (10,0),
    @DV_PERSONA CHAR (1),
    @PASAPORTE VARCHAR (20) = NULL,
    @NOMBRE_PERSONA VARCHAR (50),
    @APELLIDO_PATERNO VARCHAR (50),
    @APELLIDO_MATERNO VARCHAR (50),
    @FEC_NACIMIENTO DATE,
    @GENERO CHAR (1),
    @TELEFONO VARCHAR (20),
    @CORREO_ELECTRONICO VARCHAR (100),
    @CONTRASENA VARCHAR (255),
    @TIPO_USUARIO VARCHAR (20) = 'ESTUDIANTE',
    @CALLE VARCHAR (100),
    @NRO_CALLE VARCHAR (10),
    @ID_COMUNA INT,
    @ID_PAIS INT
    

    AS BEGIN
        SET NOCOUNT ON;

        DECLARE @EDAD INT
        SET @EDAD = DATEDIFF(YEAR, @FEC_NACIMIENTO, GETDATE());

        DECLARE @CONTRASENA_HASH VARCHAR (255)
        SET @CONTRASENA_HASH = CONVERT(VARCHAR(255), HASHBYTES('SHA2_256', @CONTRASENA));

        BEGIN TRY -- proxima vez que salga todo en rojo recuerda cerrar esta madre y volverla a abrir wladi esta cosa rancia le tiende a pasar eso
            INSERT INTO SGT_PERSONA_TALLER (RUT_PERSONA, DV_PERSONA, PASAPORTE, NOMBRE_PERSONA, APELLIDO_PATERNO, APELLIDO_MATERNO, FEC_NACIMIENTO, EDAD, GENERO, TELEFONO, CORREO_ELECTRONICO, CONTRASENA_HASH, TIPO_USUARIO, CALLE, NRO_CALLE, ID_COMUNA, ID_PAIS)
            VALUES (@RUT_PERSONA, @DV_PERSONA, @PASAPORTE, @NOMBRE_PERSONA, @APELLIDO_PATERNO, @APELLIDO_MATERNO, @FEC_NACIMIENTO, @EDAD, @GENERO, @TELEFONO, @CORREO_ELECTRONICO, @CONTRASENA_HASH, @TIPO_USUARIO, @CALLE, @NRO_CALLE, @ID_COMUNA, @ID_PAIS);

            DECLARE @ID_PERSONA NUMERIC(10,0);
            SET @ID_PERSONA = SCOPE_IDENTITY();

        --AQUI USUARIO SI DETECTA QUE ES ESTUDIANTE ASE ELLO Y AQUELLO BLASH BLUSH BOOOM EL SOL SE JUNTA CON LA LUNA Y BOOM
            IF @TIPO_USUARIO ='ESTUDIANTE'
            BEGIN
                INSERT INTO SGT_ESTUDIANTE (ID_PERSONA,AUD_USUARIO_INGRESO,AUD_FEC_INGRESO,AUD_USUARIO_MODIFICA,AUD_FEC_MODIFICA)
                VALUES (@ID_PERSONA, 'SISTEMA', GETDATE(), 'SISTEMA', GETDATE());
            END

            IF @TIPO_USUARIO ='PROFESOR'
            BEGIN
                INSERT INTO SGT_PROFESOR (ID_PERSONA,PROFESION,RESUMEN_CURRICULAR,AUD_USUARIO_INGRESO,AUD_FEC_INGRESO,AUD_USUARIO_MODIFICA,AUD_FEC_MODIFICA)
                VALUES (@ID_PERSONA, 'Por definir', 'Por definir', 'SISTEMA', GETDATE(), 'SISTEMA', GETDATE());
            END

            SELECT
                'exito' AS status, --le queria poner success por que cada vez mas me consume el js con flask
                'persona correctamente registrada' AS message,
                @ID_PERSONA AS id_persona,
                CASE @TIPO_USUARIO
                    WHEN 'ESTUDIANTE' 
                    THEN (SELECT ID_ESTUDIANTE FROM SGT_ESTUDIANTE WHERE ID_PERSONA = @ID_PERSONA)
                    WHEN 'PROFESOR' 
                    THEN (SELECT ID_PROFESOR FROM SGT_PROFESOR WHERE ID_PERSONA = @ID_PERSONA)
                ELSE NULL
            END AS id_rol;
        END TRY
        BEGIN CATCH
            SELECT
                'ERROR' AS status,
                ERROR_MESSAGE() AS message,
                NULL AS id_persona,
                NULL AS id_rol;
        END CATCH
END;
go

----

-- COSAS OBSOLETAS O DESACTUALIZADAS(SE TIENEN QUE ELIMINAR O ADAPTAR) --

USE GESTION_TALLER;

IF OBJECT_ID('INSCRIBIRSE_EN_EL_TALLER', 'P') IS NOT NULL
    DROP PROCEDURE INSCRIBIRSE_EN_EL_TALLER;
GO

CREATE PROCEDURE INSCRIBIRSE_EN_EL_TALLER
 @ID_TALLER NUMERIC(10),
 @ID_ESTUDIANTE NUMERIC(10),
 @YEAR_PROCESO INT,
 @IND_ESTADO_INTEGRANTE INT,
 @FEC_INSCRIPCION DATETIME,
 @FEC_RETIRO DATETIME,
 @OBSERVACION TEXT,
 @FEC_REINCORPORACION DATETIME,
 @AUD_USUARIO_INGRESO VARCHAR(20),
 @AUD_FEC_INGRESO DATETIME,
 @AUD_USUARIO_MODIFICA VARCHAR(20),
 @AUD_FEC_MODIFICA DATETIME

AS BEGIN
INSERT INTO SGT_INTEGRANTE_TALLER (ID_TALLER, ID_ESTUDIANTE, YEAR_PROCESO, IND_ESTADO_INTEGRANTE, FEC_INSCRIPCION, FEC_RETIRO, OBSERVACION, FEC_REINCORPORACION, AUD_USUARIO_INGRESO, AUD_FEC_INGRESO, AUD_USUARIO_MODIFICA, AUD_FEC_MODIFICA)
OUTPUT INSERTED.ID_ESTUDIANTE AS 'Su ID de estudiante', INSERTED.ID_TALLER AS 'ID de su taller', INSERTED.YEAR_PROCESO AS 'Año de ingreso'
VALUES (@ID_TALLER,@ID_ESTUDIANTE,@YEAR_PROCESO,1,GETDATE(),'1900-01-01 00:00:00.000','-','1900-01-01 00:00:00.000','','1900-01-01 00:00:00.000','','1900-01-01 00:00:00.000');

SELECT 'Los datos se han ingresado al sistema' AS RESULTADO;

END;

----

USE GESTION_TALLER;

IF OBJECT_ID('INSCRIBIR_EL_TALLER', 'P') IS NOT NULL
    DROP PROCEDURE INSCRIBIR_EL_TALLER;
GO

CREATE PROCEDURE INSCRIBIR_EL_TALLER --falta actualizar, pero no es prioridad hasta tener primero lo de estudiante terminado
@YEAR_PROCESO INT,
@ID_CATEGORIA INT,
@NOMBRE_TALLER VARCHAR(80),
@ID_DEPARTAMENTO INT,
@OBJETIVO_TALLER VARCHAR(500),
@FEC_INICIO DATE,
@FEC_TERMINO DATE,
@NRO_MINUTOS NUMERIC(10),
@NRO_CLASES_ANUAL NUMERIC(10),
@HORAS_TOTALES NUMERIC(10),
@ID_ESTADO_TALLER INT,
@FEC_ESTADO_TALLER DATE,
@OBSERVACION VARCHAR(50),
@LUGAR VARCHAR(100),
@MINIMO_ESTUDIANTE INT,
@MAXIMO_ESTUDIANTE INT,
@REQUISITO VARCHAR(500),
@EDAD_MINIMA INT,
@EDAD_MAXIMA INT,
@MATERIAL VARCHAR(500),
@IND_TIPO_TALLER INT,
@AUD_USUARIO_INGRESO VARCHAR(20),
@AUD_FEC_INGRESO DATETIME,
@AUD_USUARIO_MODIFICA VARCHAR(20),
@AUD_FEC_MODIFICA DATETIME

AS BEGIN
SET IDENTITY_INSERT SGT_TALLER ON;
INSERT INTO SGT_TALLER (YEAR_PROCESO,ID_CATEGORIA,NOMBRE_TALLER,ID_DEPARTAMENTO,OBJETIVO_TALLER,FEC_INICIO,FEC_TERMINO,NRO_MINUTOS,NRO_CLASES_ANUAL,HORAS_TOTALES,ID_ESTADO_TALLER,FEC_ESTADO_TALLER,OBSERVACION,LUGAR,MINIMO_ESTUDIANTE,MAXIMO_ESTUDIANTE,REQUISITO,EDAD_MINIMA,EDAD_MAXIMA,MATERIAL,IND_TIPO_TALLER,AUD_USUARIO_INGRESO,AUD_FEC_INGRESO,AUD_USUARIO_MODIFICA,AUD_FEC_MODIFICA)
OUTPUT INSERTED.ID_TALLER AS 'ID de su taller',INSERTED.NOMBRE_TALLER AS 'Nombre de su taller'
VALUES (@YEAR_PROCESO,@ID_CATEGORIA,@NOMBRE_TALLER,@ID_DEPARTAMENTO,@OBJETIVO_TALLER,@FEC_INICIO,@FEC_TERMINO,@NRO_MINUTOS,@NRO_CLASES_ANUAL,@HORAS_TOTALES,@ID_ESTADO_TALLER,GETDATE(),'-',@LUGAR,@MINIMO_ESTUDIANTE,@MAXIMO_ESTUDIANTE,@REQUISITO,@EDAD_MINIMA,@EDAD_MAXIMA,@MATERIAL,@IND_TIPO_TALLER,'','1900-01-01 00:00:00.000','','1900-01-01 00:00:00.000');
SET IDENTITY_INSERT SGT_TALLER OFF;

SELECT 'El taller a sido ingresado al sistema' AS RESULTADO;

END;

----

USE GESTION_TALLER;

IF OBJECT_ID('INSCRIBIR_TALLERISTAS', 'P') IS NOT NULL
    DROP PROCEDURE INSCRIBIR_TALLERISTAS;
GO

CREATE PROCEDURE INSCRIBIR_TALLERISTAS --falta actualizar, pero no es prioridad hasta tener primero lo de estudiante terminado
@ID_PERSONA NUMERIC(10),
@PROFESION VARCHAR(250),
@RESUMEN_CURRICULAR VARCHAR(250),
@AUD_USUARIO_INGRESO VARCHAR(250),
@AUD_FEC_INGRESO DATETIME,
@AUD_USUARIO_MODIFICA VARCHAR(250),
@AUD_FEC_MODIFICA DATETIME

AS BEGIN
INSERT INTO SGT_PROFESOR (ID_PERSONA, PROFESION, RESUMEN_CURRICULAR, AUD_USUARIO_INGRESO, AUD_FEC_INGRESO, AUD_USUARIO_MODIFICA, AUD_FEC_MODIFICA)
OUTPUT INSERTED.ID_PROFESOR AS 'Su ID de profesor', INSERTED.ID_PERSONA AS 'Su ID de persona'
VALUES (@ID_PERSONA,@PROFESION,@RESUMEN_CURRICULAR,'','1900-01-01 00:00:00.000','','1900-01-01 00:00:00.000');
SELECT 'Se ingreso datos de "Profesor/a" al sistema' AS RESULTADO;

END;

----

USE GESTION_TALLER;

IF OBJECT_ID('VER_ESTUDIANTE', 'P') IS NOT NULL
    DROP PROCEDURE VER_ESTUDIANTE;
GO

CREATE PROCEDURE VER_ESTUDIANTE
    @ID_ESTUDIANTE NUMERIC(10)
AS 
BEGIN
    SELECT 
        E.ID_TALLER,
        T.NOMBRE_TALLER,
        E.ID_ESTUDIANTE,
        E.YEAR_PROCESO,
        E.IND_ESTADO_INTEGRANTE,
        E.FEC_INSCRIPCION,
        E.FEC_RETIRO,
        E.OBSERVACION,
        E.FEC_REINCORPORACION,
        E.AUD_USUARIO_INGRESO,
        E.AUD_FEC_INGRESO,
        E.AUD_USUARIO_MODIFICA,
        E.AUD_FEC_MODIFICA
    FROM SGT_INTEGRANTE_TALLER E
    INNER JOIN SGT_TALLER T 
        ON E.ID_TALLER = T.ID_TALLER
    WHERE E.ID_ESTUDIANTE = @ID_ESTUDIANTE
    ORDER BY E.FEC_INSCRIPCION DESC;
END;

----

USE GESTION_TALLER;

IF OBJECT_ID('VER_PROFESOR', 'P') IS NOT NULL
    DROP PROCEDURE VER_PROFESOR;
GO

CREATE PROCEDURE VER_PROFESOR --falta actualizar, pero no es prioridad hasta tener primero lo de estudiante terminado
@ID_PROFESOR NUMERIC(10)

AS BEGIN
SELECT E.ID_PERSONA AS 'ID de la persona',E.PROFESION AS 'Profesion',E.RESUMEN_CURRICULAR AS 'Resumen curricular',I.ID_TALLER AS 'ID del taller',O.NOMBRE_TALLER AS 'Noombre del taller',E.AUD_FEC_INGRESO AS 'Fecha de ingreso',E.AUD_FEC_MODIFICA AS 'Fecha modificada'
FROM SGT_PROFESOR A
INNER JOIN SGT_PROFESOR E
	ON A.ID_PROFESOR=E.ID_PROFESOR
INNER JOIN SGT_GESTION_TALLER I
	ON A.ID_PROFESOR=I.ID_PROFESOR
INNER JOIN SGT_TALLER O
	ON I.ID_TALLER=O.ID_TALLER
WHERE E.ID_PROFESOR=@ID_PROFESOR;

END;

----

USE GESTION_TALLER;

IF OBJECT_ID('VER_TALLERES', 'P') IS NOT NULL --falta actualizar, pero no es prioridad hasta tener primero lo de estudiante terminado
    DROP PROCEDURE VER_TALLERES;
GO

CREATE PROCEDURE VER_TALLERES
    @ID_TALLER NUMERIC(10)
AS 
BEGIN
    SELECT 
        A.ID_TALLER AS 'ID del taller',
        A.ID_CATEGORIA AS 'ID de la categoria',
        A.NOMBRE_TALLER AS 'Nombre del taller',
        A.ID_DEPARTAMENTO AS 'ID del departamento',
        A.OBJETIVO_TALLER AS 'Objetivo del taller',
        A.FEC_INICIO AS 'Fecha de inicio',
        A.FEC_TERMINO AS 'Fecha de termino',
        A.NRO_MINUTOS AS 'Numero de minutos',
        A.NRO_CLASES_ANUAL AS 'Numero de clases anuales',
        A.HORAS_TOTALES AS 'Horas totales',
        A.ID_ESTADO_TALLER AS 'ID del estado del taller',
        A.FEC_ESTADO_TALLER AS 'Fecha del estado del taller',
        A.OBSERVACION AS 'Observacion',
        A.LUGAR AS 'Lugar',
        A.MINIMO_ESTUDIANTE AS 'Minimo de estudiantes',
        A.MAXIMO_ESTUDIANTE AS 'Maximo de estudiantes',
        COUNT(E.ID_ESTUDIANTE) AS 'Personas inscritas',
        A.REQUISITO AS 'Requisitos',
        A.EDAD_MINIMA AS 'Edad minima',
        A.EDAD_MAXIMA AS 'Edad maxima',
        A.MATERIAL AS 'Materiales'
    FROM SGT_TALLER A
    LEFT JOIN SGT_INTEGRANTE_TALLER E
        ON A.ID_TALLER = E.ID_TALLER
    WHERE A.ID_ESTADO_TALLER IN (1,2)
      AND A.ID_TALLER = @ID_TALLER
    GROUP BY
        A.ID_TALLER,
        A.ID_CATEGORIA,
        A.NOMBRE_TALLER,
        A.ID_DEPARTAMENTO,
        A.OBJETIVO_TALLER,
        A.FEC_INICIO,
        A.FEC_TERMINO,
        A.NRO_MINUTOS,
        A.NRO_CLASES_ANUAL,
        A.HORAS_TOTALES,
        A.ID_ESTADO_TALLER,
        A.FEC_ESTADO_TALLER,
        A.OBSERVACION,
        A.LUGAR,
        A.MINIMO_ESTUDIANTE,
        A.MAXIMO_ESTUDIANTE,
        A.REQUISITO,
        A.EDAD_MINIMA,
        A.EDAD_MAXIMA,
        A.MATERIAL;
END;
GO

----

USE GESTION_TALLER;

IF OBJECT_ID('INSCRIBIR_TALLER_O_ESPERA', 'P') IS NOT NULL
    DROP PROCEDURE INSCRIBIR_TALLER_O_ESPERA;
GO

CREATE PROCEDURE INSCRIBIR_TALLER_O_ESPERA
    @ID_TALLER NUMERIC(10),
    @ID_ESTUDIANTE NUMERIC(10),
    @YEAR_PROCESO INT
AS 
BEGIN
    SET NOCOUNT OFF;
    
    DECLARE @CUPOS_DISPONIBLES INT;
    DECLARE @CUPOS_MAXIMOS INT;
    DECLARE @INSCRITOS INT;
    DECLARE @NUEVO_ID_ESPERA NUMERIC;
    DECLARE @MENSAJE VARCHAR(500);
    DECLARE @EN_LISTA_ESPERA BIT;

    SELECT @CUPOS_MAXIMOS = MAXIMO_ESTUDIANTE 
    FROM SGT_TALLER 
    WHERE ID_TALLER = @ID_TALLER;

    IF @CUPOS_MAXIMOS IS NULL
    BEGIN
        SELECT 'El taller especificado no existe' AS mensaje, 0 AS en_lista_espera, @ID_TALLER AS id_taller, @ID_ESTUDIANTE AS id_estudiante;
        RETURN;
    END

    SELECT @INSCRITOS = COUNT(*) 
    FROM SGT_INTEGRANTE_TALLER 
    WHERE ID_TALLER = @ID_TALLER 
    AND IND_ESTADO_INTEGRANTE = 1;

    SET @CUPOS_DISPONIBLES = @CUPOS_MAXIMOS - @INSCRITOS;
    IF EXISTS (SELECT 1 FROM SGT_INTEGRANTE_TALLER 
               WHERE ID_ESTUDIANTE = @ID_ESTUDIANTE 
               AND ID_TALLER = @ID_TALLER
               AND IND_ESTADO_INTEGRANTE = 1)
    BEGIN
        SET @MENSAJE = 'El estudiante ya está inscrito en este taller';
        SET @EN_LISTA_ESPERA = 0;
    END
    ELSE IF @CUPOS_DISPONIBLES > 0
    BEGIN
        INSERT INTO SGT_INTEGRANTE_TALLER (ID_TALLER, ID_ESTUDIANTE, YEAR_PROCESO, IND_ESTADO_INTEGRANTE, FEC_INSCRIPCION, FEC_RETIRO, OBSERVACION, FEC_REINCORPORACION, AUD_USUARIO_INGRESO, AUD_FEC_INGRESO, AUD_USUARIO_MODIFICA, AUD_FEC_MODIFICA) 
        VALUES (@ID_TALLER, @ID_ESTUDIANTE, @YEAR_PROCESO,1, GETDATE(), '1900-01-01 00:00:00.000', '-', '1900-01-01 00:00:00.000','sistema', GETDATE(), 'sistema', GETDATE());
        SET @MENSAJE = 'Inscripción exitosa';
        SET @EN_LISTA_ESPERA = 0;
    END
    ELSE
    BEGIN
        IF OBJECT_ID('SGT_LISTA_ESPERA', 'U') IS NOT NULL
        BEGIN
            INSERT INTO SGT_LISTA_ESPERA (ID_TALLER, ID_ESTUDIANTE, YEAR_PROCESO,IND_ESTADO_INTEGRANTE, FECHA_ESPERA, ESTADO) 
            VALUES (@ID_TALLER, @ID_ESTUDIANTE, @YEAR_PROCESO,1, GETDATE(), 'En Espera');
            SET @NUEVO_ID_ESPERA = SCOPE_IDENTITY();
            SET @MENSAJE = 'No hay cupos disponibles. Estudiante agregado a lista de espera (ID: ' + CAST(@NUEVO_ID_ESPERA AS VARCHAR) + ')';
            SET @EN_LISTA_ESPERA = 1;
        END
        ELSE
        BEGIN
            SET @MENSAJE = 'No hay cupos disponibles en el taller';
            SET @EN_LISTA_ESPERA = 1;
        END
    END;
    SELECT @MENSAJE AS mensaje, @EN_LISTA_ESPERA AS en_lista_espera, @ID_TALLER AS id_taller, @ID_ESTUDIANTE AS id_estudiante;
END;
GO

----

USE GESTION_TALLER;

IF OBJECT_ID('AC_TALLERISTAS', 'P') IS NOT NULL
    DROP PROCEDURE AC_TALLERISTAS;
GO

CREATE PROCEDURE AC_TALLERISTAS --falta actualizar, pero no es prioridad hasta tener primero lo de estudiante terminado
@ID_PERSONA NUMERIC(10),
@PROFESION VARCHAR(250),
@RESUMEN_CURRICULAR VARCHAR(250),
@AUD_USUARIO_INGRESO VARCHAR(250),
@AUD_FEC_INGRESO DATETIME,
@AUD_USUARIO_MODIFICA VARCHAR(250),
@AUD_FEC_MODIFICA DATETIME

AS BEGIN

UPDATE SGT_PROFESOR
SET PROFESION=@PROFESION,
	RESUMEN_CURRICULAR=@RESUMEN_CURRICULAR,
	AUD_USUARIO_INGRESO=@AUD_USUARIO_INGRESO,
	AUD_FEC_INGRESO=@AUD_FEC_INGRESO,
	AUD_USUARIO_MODIFICA=@AUD_USUARIO_MODIFICA,
	AUD_FEC_MODIFICA=@AUD_FEC_MODIFICA
WHERE ID_PERSONA=@ID_PERSONA

SELECT 'Se han actualizado los datos del/la "Profesor/a" al sistema' AS RESULTADO;
END;

----

USE GESTION_TALLER;

IF OBJECT_ID('AC_TALLER', 'P') IS NOT NULL
    DROP PROCEDURE AC_TALLER;
GO

CREATE PROCEDURE AC_TALLER --falta actualizar, pero no es prioridad hasta tener primero lo de estudiante terminado
@ID_TALLER INT,
@YEAR_PROCESO INT,
@ID_CATEGORIA INT,
@NOMBRE_TALLER VARCHAR(80),
@ID_DEPARTAMENTO INT,
@OBJETIVO_TALLER VARCHAR(500),
@FEC_INICIO DATE,
@FEC_TERMINO DATE,
@NRO_MINUTOS NUMERIC(10),
@NRO_CLASES_ANUAL NUMERIC(10),
@HORAS_TOTALES NUMERIC(10),
@ID_ESTADO_TALLER INT,
@FEC_ESTADO_TALLER DATE,
@OBSERVACION VARCHAR(50),
@LUGAR VARCHAR(100),
@MINIMO_ESTUDIANTE INT,
@MAXIMO_ESTUDIANTE INT,
@REQUISITO VARCHAR(500),
@EDAD_MINIMA INT,
@EDAD_MAXIMA INT,
@MATERIAL VARCHAR(500),
@IND_TIPO_TALLER INT,
@AUD_USUARIO_INGRESO VARCHAR(20),
@AUD_FEC_INGRESO DATETIME,
@AUD_USUARIO_MODIFICA VARCHAR(20),
@AUD_FEC_MODIFICA DATETIME

AS BEGIN
UPDATE SGT_TALLER
SET ID_ESTADO_TALLER=@ID_ESTADO_TALLER,
	FEC_ESTADO_TALLER=@FEC_ESTADO_TALLER,
	OBSERVACION=@OBSERVACION,
	AUD_USUARIO_INGRESO=@AUD_USUARIO_INGRESO,
	AUD_FEC_INGRESO=@AUD_FEC_INGRESO,
	AUD_USUARIO_MODIFICA=@AUD_USUARIO_MODIFICA,
	AUD_FEC_MODIFICA=@AUD_FEC_MODIFICA
WHERE ID_TALLER=@ID_TALLER

SELECT 'El taller a sido actualizado en el sistema' AS RESULTADO;

END;

----

el login actualmente se encuentra funcional puede recibir usuarios y los deja en el dashboard del funcionario, el tema esta ahora que se tiene que cumplir los requerimientos que me dieron que son
Administracion de talleres
1.- Revision de inscripciones
2.- Mantenedores 
3.- Crud Talleristas
4.- Crud Talleres
5.- Crud inscripciones

6.- Masivo Talleres
7.- Lista de espera funcional
8.- Gestion de cupos

te estoy pasando todo lo que tengo , el proyecto debe ser algo que tenga seguridad y que tenga un aspecto serio, no es ncesario que en los bordes de cada cosa este redondeado, eso me da asco de ver
el funcionario sera el que use el programa , el login es para que el funcionario tenga acceso a este mismo, y lo que puede hacer esta en los requerimientos que te pase, eso es lo que deberia ser
capaz de hacer un funcionario, sin mencionar que se deben cumplir altos estandares de un proyecto informatico, debe tener carpetas para los logs de errores como ya esta, debe ser seguro ante ataques
y inyecciones, debe estar vien segementando el codigo con tal de que si se presenta un fallo o posible modificacion este listo para una deteccion rapida. el proyecto en si es para gente que sera
de una municipalidad asi que es para personas que ya saben como va el asunto y que si tienen problemas no necesitan un fono o algo, saben donde esta el equipo de informatica para pedir ayuda y/o
modificaciones necesarias. quiero ver primero el html, js y los .py, el css los quiero solo cuando yo te los pida, sin embargo dame un css basico pero muy basico para que se puedan separar las casillas
de las cosas que se vea necesario, si es posible la vista para ver los talleres , talleristas , personas que asisten a los talleres y las inscripciones verlas en paginas distintas y que se muestre
al ingresar a dicha pagina

IMPORTANTE
revisa la BASE_DE_DATOS/Esquema de archivos. se esta usando sql server version 2008 r2 para esto, tambien asegurate de comentar mucho asi se que estas haciendo, no borres mis comentarios, adaptalos a los que estes cambiando
o añadiendo, muy probablemente falles tengas muchos erroes, eso le saldra dinero a tus creadores asi que espero un buen trabajo de ti, no falles y no le hagas perder recursos a tus creadores.